# https://circleci.com/blog/building-and-testing-an-asp-net-core-application/
# Currently CircleCI supports a single Windows image: Windows Server 2019 with Visual Studio 2019.

version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:9.6.1
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "18:29:20:fa:30:07:2f:0a:78:9b:0e:de:9f:b6:fd:fe"
      - run: git commit --allow-empty -m "[skip ci] Test pushback" && git push origin master

