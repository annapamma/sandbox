version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:9.6.1
    resource_class: large
    steps:
      - checkout
      - run: echo 'i am building and it requires a ton of memory'
  test:
    docker:
      - image: circleci/node:9.6.1
    parallelism: 3
    steps:
      - checkout
      - run: echo 'i am running A LOT OF tests'

workflows:
  build-then-test:
    jobs:
      - build
      - test:
          requires:
            - build

# follow project
# checkout
# hello world
# jobs
# parallelism: https://circleci.com/gh/annapamma/sandbox/189
# resource classes:
# workflows: https://circleci.com/gh/annapamma/sandbox/186
# caching
