version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  build:
    executor:
      name: win/default
    steps:
      - checkout
      - run:
          name: "Check docker"
          shell: powershell.exe
          command: |
            choco install python --version=3.8.0