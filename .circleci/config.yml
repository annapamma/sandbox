version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:9.6.1
    resource_class: small
    steps:
      - run: exit 1

workflows:
  build-then-test:
    jobs:
      - build
      - test:
          requires:
            - build


# follow project
# checkout: ~/project by default, can set working_dir key
# jobs
# workflows: https://circleci.com/gh/annapamma/sandbox/186
# parallelism: https://circleci.com/gh/annapamma/sandbox/189
# resource classes: https://circleci.com/gh/annapamma/sandbox/191
# caching
# before: https://circleci.com/gh/annapamma/sandbox/211
# after: https://circleci.com/gh/annapamma/sandbox/213
# bonus: reusable config
