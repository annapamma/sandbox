version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:9.6.1
    parallelism: 3
    steps:
      - checkout
      - run: echo 'hello world'

# follow project
# checkout
# hello world
# jobs
# workflows
# parallelism
# resource classes
# caching
# save artifacts