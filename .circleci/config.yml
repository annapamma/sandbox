version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:9.6.1
    steps:
      - run: echo 'runs when PR is made'
  do-not-build-on-pr-a:
    docker:
      - image: circleci/node:9.6.1
    steps:
      - run: echo "should not have built"

workflows:
  ignore-pr-b: # this should not be added as a check to pr-a
    jobs:
      - build
      - do-not-build-on-pr-a:
        filters:
          branches:
            ignore:
              - pr-a